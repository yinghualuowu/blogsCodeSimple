<Window x:Class="Panel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Binding"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Grid UseLayoutRounding="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>

        </StackPanel>
        <WrapPanel Grid.Row="1" ItemHeight="100" ItemWidth="100">
            <Button/>
            <Button/>
            <Button/>
        </WrapPanel>
        <DockPanel Grid.Row="2">
            <Button Width="100" DockPanel.Dock="Right"></Button>
            <Button Width="100" DockPanel.Dock="Left"></Button>
        </DockPanel>
        <UniformGrid Grid.Row="3" Rows="2" Columns="2">

        </UniformGrid>
        <Canvas Grid.Row="4"></Canvas>
    </Grid>
</Window>
